---

---

<article>
	<div class='container'>
		<div class='line-top'></div>
		<h1>HIROYUKI9614</h1>
		<div class='line-bottom'></div>
		<p>のページです。</p>
		<p>自己紹介やポートフォリオの紹介と兼ねて、</p>
		<p>日々の学習のアウトプットの場として活用しています。</p>
	</div>
</article>

<style>
	@import url('https://fonts.googleapis.com/css2?family=Bungee&display=swap');
	article {
		display: flex;
		justify-content: center;
	}

	h1 {
		opacity: 0;
		font-family: 'Bungee', sans-serif;
		font-size: clamp(2.2rem, 0.091rem + 10.55vw, 8rem);
		text-align: center;
		margin-bottom: 1rem;
		color: yellow;
		animation:
			shadow-change 3s ease-in-out forwards,
			fade-in 2.5s ease-in-out forwards;
	}

	@keyframes shadow-change {
		0% {
			text-shadow: 0 0 0 #dc00fc;
		}
		80% {
			text-shadow: 0 0 0 #dc00fc;
		}
		100% {
			text-shadow: 3px 4px 5px #dc00fc;
		}
	}

	@keyframes fade-in {
		0% {
			opacity: 0;
		}
		60% {
			opacity: 0;
		}
		100% {
			opacity: 1;
		}
	}

	.container {
		position: relative;
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	.line-top {
		position: absolute;
		top: 18%;
		/* right: 50%; */
		width: 5rem;
		height: 5rem;
		border-top: 2px solid white;
		border-left: 2px solid white;
		animation: rotation1 0.5s linear forwards;
	}

	.line-bottom {
		position: absolute;
		top: 18%;
		right: 50%;
		width: 5rem;
		height: 5rem;
		border-bottom: 2px solid white;
		border-right: 2px solid white;
		animation:
			0.5s linear forwards rotation1,
			2s linear forwards move-right;
	}

	@keyframes move-right {
		0% {
			transform: rotate(0);
		}
		100% {
			right: 50%;
		}
	}

	@keyframes rotation1 {
		0% {
			transform: rotate(0);
		}
		100% {
			transform: rotate(90deg);
		}
	}
</style>
